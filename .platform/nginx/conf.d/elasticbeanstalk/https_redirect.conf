# Redirect plain-HTTP requests to HTTPS. TLS terminates at the ALB, which sets
# X-Forwarded-Proto. ALB health checks don't send that header, so they still
# reach the app over HTTP and aren't redirected.
if ($http_x_forwarded_proto = "http") {
    return 301 https://$host$request_uri;
}
